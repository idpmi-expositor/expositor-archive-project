# Legacy Generated Outputs

This tree is non-canonical legacy/generated output from the `ExpositorMain` sync/import layout.

Do not treat any file under this directory as archival truth, even if it appears under `archive/lessons`, `indexes`, `schemas`, `normalized`, `structured`, or `ocr` subdirectories.

The canonical pipeline paths live at the repository root. Reviewed canonical lesson YAML exists only under root `archive/lessons` after human review and schema validation.
